# 2026-08-07 日志（Day 7/30）

> 模板启用第 7 天。本条目截至 8/8 晨（含周五晚），时间块为**估算**。

## 时间分配（每个块一行：起止 | 层级 | 做了什么）
- 08:00-12:00 | L2 | PyTorch 跟教程 hand-write 最小神经网络：造非线性数据 → Linear+ReLU 前向 → MSE loss → autograd 反向传播 → 优化器迭代；绘图看到拟合、loss 下降
- 14:00-17:00 | L1 | Pi Agent 框架深化：读三层架构（依赖漏斗分层法），厘清 Pi-AI / Agent core / Pi coding agent 三层单向依赖与解耦原则
- 20:00-21:30 | L4 | 电磁学：尖端放电现象，用导体表面电荷密度（σ 高 → E=σ/ε₀ 高 → 空气击穿电离）解释成因，接 8/6 静电平衡

## 今日主事（最多 5 件，写具体动作不是类别）
1. PyTorch 跟教程 hand-write 最小 NN（forward+loss+backward+train loop），跑通并可视化，理解训练大致流程（L2 冲刺 step 1-3）
2. Pi Agent 三层架构深化：厘清 Pi-AI / Agent core / Pi coding agent 的单向依赖 + 解耦/可独立测试/可换 LM provider 的设计原则（L1 稳）
3. 晚间 L4：尖端放电现象，用导体表面电荷密度 σ→E=σ/ε₀→空气击穿 的第一性原理解释成因（接 8/6 静电平衡）

## 能力自评（1-5，基于今日/近期体感，不是考试分）
L1: 2 | L2: 4 | L3: 2 | L4: 2 | L5: 3
> L2 上调到 4：今上午真实 hand-write 并跑通一个 NN（非只 Explain），迈出 Implement 第一步；但"逐行讲清"尚未自证，故不到 5。L1=2 因 Pi 仍多 ⬜ 未 hand-write；L4/L5 沿用基线。

## 偏离检查（对照硬规则，✓=守住了 ✗=破了，破了的写一句原因）
- [✓] L1/L2 落在上午 8-14 窗口（PyTorch 在 08-12，合规，未放晚上）
- [✓] 序列化：今日 L2 推力只在上午 PyTorch；下午 Pi 属 L1 稳（读架构，未 hand-write L2），全天仅一个 L2 推进，合规
- [✓] 一天主事 ≤ 5、层级 ≤ 3（3 主事 / L1+L2+L4 三层级，合规）
- [✓] 晚间只碰 L4（尖端放电），未学 L1/L2 新内容，落在晚上窗口，合规
- 备注：上午 PyTorch（L2 推力）+ 下午 Pi（L1 稳）+ 晚间 L4（尖端放电），全天仅一个 L2 推进、层级 L1+L2+L4，符合"序列化 + 深度优先"，晚间守 L4 边界。

## 待复盘卡点（active learning）
（历史 ⬜ 沿用）
- ⬜ 消费流（consumption stream）是什么：Pi 里 Event Stream 之外的"消费流"（L1, 8/4）
- ⬜ Pi Agent agent loop 的 async/await 底层（L1, 8/2）
- 🟡 ReLU 前向正确位置 + 真实作用（L2, 8/5）：今上午 hand-write 前向传播含 ReLU，**待逐行讲清闭环**（讲清即翻 ✅）
- ⬜ bias（IEEE754 偏移）为何如此设计（L3, 7/31）
- ⬜ 带电杆左右电场方向（L4, 7/31，可借 E=−∇V 闭环）

---

## 上午（L2 实现 / PyTorch 最小 NN 跟练）

### 做了什么
- 跟教程把一段 PyTorch 代码从头敲了一遍，不是只看。流程串起来了：
  1. **造非线性数据**：生成一组非线性（曲线）样本，用来逼网络必须靠非线性激活才能拟合。
  2. **建模+前向传播**：用 Linear 层 + 激活函数（ReLU 之类）搭小网络，输入往前算到输出。
  3. **损失函数**：用 MSE 衡量预测和真值的差距。
  4. **反向传播 + 优化**：autograd 自动算梯度，优化器据梯度更新参数；迭代几步后 loss 下降。
  5. **绘图验证**：把拟合结果画出来，看到网络曲线逐渐贴住原始非线性数据。
- 直观上把"训练一个神经网络的大致过程"跑通了一遍：数据 → 前向 → 算 loss → 反向 → 更新 → 再前向，循环到收敛。

### Agent 连接（冲刺判定）
- 这正好是本周冲刺 #1「PyTorch L1→L2」的 step 0-3：读基础 → hand-write forward → hand-write loss+backward+train loop → 跑通 loss 下降。dashboard 已把 done 标到 [true,true,true,false]。
- **但 L2 的真门是 exit 里的"逐行讲清"**。你跟着教程敲、跑通、看到图，这是真 Implement 的起步，不是 L1 的 Only-Explain。可要确认它不是"手会敲、脑子没走通"，得你能把下面几点用自己的话讲顺（这就是 step 4 的 ⬜ 门）：
  1. 为什么非线性数据必须用激活函数？没有 ReLU 会怎样？
  2. autograd / 反向传播到底在算什么、梯度怎么来的？
  3. 优化器拿梯度干了什么、loss 为什么能降？
- 讲清这三点，这张卡就翻 ✅，L2 算真正到手。

### 下一步
- 下午若继续：保持序列化，要么把 PyTorch 的"逐行讲清"自证掉（翻 L2 门），要么切到 Pi 源码读消费流/async（但同一天只碰一个 L2，所以二选一）。
- 别在跑通图就停，关键在"讲清"。

---

## 下午（L1 框架 / Pi Agent 三层架构深化）

### 学了什么
- 顺着昨天看清的单向依赖，今天用「依赖漏斗分层法」把三层对上了具体名字与职责：
  - **Pi-AI（最底）**：只调 LLM。你在这层要自己管状态、自己写循环，但它最通用、最不受业务绑定。
  - **Agent core（中间）**：在 Pi-AI 之上叠加，给出"完整的 agent 能力"（循环、状态机、工具调度等）。但工具、入口还要你自己写，所以它适合做成你自己的独立 agent / 工具，而不是开箱即用的产品。
  - **Pi coding agent（最顶）**：三层齐备，就是像 Pi 本身那样的编程助手，可以直接用，也能在其上扩展能力。
- 方向很关键：**依赖只向下**（上层依赖下层，下层不知道上层存在）；越往上越具体、越往下越通用。这就是"漏斗"——底部宽（通用能力），顶部窄（具体业务）。

### 设计原则（他自己的提炼）
- **解耦每层**：不能把能力耦合在一起。实操判据很硬——"假设要换 LM provider，是得重写一整个文件，还是底层已经有通用能力、只改一小块？" 如果换 provider 要动全身，就是耦合了。
- **每层可独立测试**：依赖关系明确，单层能单独跑、单独验证，改起来不会牵一发动全身。
- **易修改 / 易扩展 / 易使用**：这正是 Pi 架构高明的地方——把"通用"和"具体"分层放好，未来换模型、加工具、接新场景都只碰对应那一层。

### Agent 连接（冲刺判定）
- 这依然是 **L1（Explain）**：你把三层关系、依赖方向、设计动机讲得很顺，但代码不是你写的，而且两个 ⬜ 卡点（消费流、async 底层）还没去源码里定位。所以 Pi 这周目标是"L1 稳 → 撞 L2"，今天是在把 L1 底座补厚、补到能讲清"为什么这么分"。
- **L2 的真门没变**：hand-write 一个最小 core loop + 1 个工具 + 接 LLM，跑通一个任务。今天的理解是写它之前必要的地图——等你动手时，这些分层就是你文件该怎么切的直接依据。
- 顺带确认序列化守住了：今天 L2 推力只在上午 PyTorch，下午 Pi 是 L1 稳，全天只推了一个 L2，合规。

### 下一步
- PyTorch 的"逐行讲清"还没自证（上午那三个问题仍 ⬜），这是当前最该翻的门，别被 Pi 的爽感挤掉。
- Pi 要真正撞 L2，下一步是去源码定位消费流 ⬜ 与 async ⬜（读通就翻 ⬜），再 hand-write 最小 loop。今天的架构图正好给这一步打了底。

---

## 晚间（L4 数理 / 尖端放电现象 + 电荷密度解释）

### 学了什么
- 周五晚继续电磁学：研究**尖端放电（电晕放电）**现象，并用**电荷密度**把它从第一性原理讲通了。
- 解释链（直接接 8/6 静电平衡）：
  1. 静电平衡时导体表面电荷分布不均匀——曲率大的地方（尖端）**表面电荷密度 σ 大**。
  2. 导体表面外侧紧邻处电场垂直于表面，大小由高斯定理给出 **E = σ / ε₀**。
  3. 尖端 σ 极高 → 局部 E 远超空气击穿场强（约 3×10⁶ V/m）→ 空气被电离 → 形成电晕、把电荷泄放掉。
  4. 所以"尖端放电"本质是：电荷在曲率大的位置拥挤 → 强场把周围空气变成导体 → 电荷经电离空气跑掉。
- 这把 8/6 那套（导体表面 E 垂直、σ 分布、等势体）从"静"的静电平衡，推到了"动"的非平衡放电，是一条连贯的 L4 推导链。

### Agent 连接
- 仍是 **L4（Explain / 推导）**：你用 σ→E→电离 的链条自己解释了现象，不是背结论，这正是 L4 该有的样子。可再深一层自问：**为什么 σ 在尖端更大？**（平衡时表面等势，小半径处要维持同电势，局部 σ 必须更高；或直觉说同号电荷互斥、往最外凸处挤）——把这一环也讲顺，这条链就彻底闭环。
- 边界合规：晚间学的是 L4（非 L1/L2），落在晚上窗口，未碰 L1/L2 新内容，符合硬规则。
- 与 7/31 那条 ⬜「带电杆左右电场方向」同源（都是导体表面场 + σ 分布），本条目不强制翻，但今晚的理解已为它打了更厚的底。

### 下一步
- PyTorch「逐行讲清」3 问仍 ⬜（最该翻的门），挑个整块时间收掉，PyTorch L2 即到手。
- Pi 撞 L2 仍是去源码翻消费流⬜/async⬜ + hand-write 最小 loop。
- L4 这条 σ→E→放电 链，若想把"为什么尖端 σ 大"也推导一遍，就彻底闭环。
