# 2026-07-30 学习日志

## 今天推进了什么
- 上午（L5 语言 / 英语）：用 YouTube 可理解输入法（comprehensible input）学词汇——靠语境 + 视觉信息推断词义，不硬背。
- 上午（L2 深度学习 / LLM 原理）：继续 DL 理论学习。
- 上午（L1 落地规划）：产生「自己实现一个 mini-GPT」的想法，决定**边学理论边写 lab**——用代码暴露理解盲区，对齐「手写代码、不让 AI 代办」的学习原则。
- 休息：连着两天较累，完成任务后补了一觉。
- 元认知讨论：与 AI 讨论学习的未来与身份认同，提出类比对「人的思维 / 行为模式 ≈ 训练一个神经网络 / 语言模型」——高质量输入 → 模型层 → 决策层 → 反馈 → 调整。

## 关键认知 / 纠正
- mini-GPT 落地思路对：光看概念很多东西落不了地，用 lab 逼出盲区，正是你定的「手写代码」原则，也是 L1（Agent 框架）与 L2（LLM 原理）的衔接点。
- 类比亮点——你说的「反馈 / 调整」环节，最精确对应的是**昨天学的 RLHF**：人靠奖励 / 惩罚（操作性条件反射）调整行为，不是监督学习的「标准答案标签」。所以「人 ≈ 用 RLHF 训出来的模型」比「人 ≈ 监督训练」更准。「高质量输入」那环也成立：垃圾进垃圾出，吸收的信息质量决定「模型（你）」的上限。
- 类比边界（别当真）：人不是真的在反向传播；人的「权重更新」是神经可塑性 + 习惯养成，慢、模糊、带情绪。类比是建立直觉的工具，不是还原论。
- 精力管理：累了就睡，没问题。上午其实推进了「语言 + DL + mini-GPT 决策 + 一次高质量元认知讨论」，产出不低，别因「没做太多」自责。

## 证据或结果
- mini-GPT 计划已定（尚未动手写）。
- 类比对自洽，且与 07-29 学的 RLHF 闭环呼应。

## 下午追加（18:16）
- 下午（L1/L2 落地）：本打算动手手搓 mini-GPT，开工后发现前置知识不足——PyTorch 不熟，回去补 PyTorch。
- 工具踩坑：用 Claude Code 辅助时，它反复在「帮你配置环境」上折腾，引出一堆问题，且**完全抓不住我的知识状态 / 进度**，瞎配置浪费了大量时间。
- 关键洞察（元认知）：当前 AI agent 在「建模用户的知识状态与学习进度」这一点上做得很差——它不知道你缺什么、会什么，于是要么过度代劳、要么瞎指挥。这反向说明：维持一份跨会话的「知识状态记录」（如本日志 + 记忆文件）正是补这个短板的解法；也正好是 L1 里 agent 的 Memory 模块要解决的问题。

## 下一步最自然的动作
- 先把 PyTorch 补到能流畅写 tensor / autograd / nn.Module 的程度（这是 mini-GPT 的前置，当前阻塞项）；PyTorch 扎实后再回到 mini-GPT 最小闭环。
- 动手写 mini-GPT：先跑通最小可跑闭环——字符级 token + embedding + 单层 attention + 交叉熵，目标「预测下一个字符」，跑通后再堆深度。这能直接验证 07-29 的「自监督造标签」：GPT 的标签就是「下一个 token」，数据自己造的。核心逻辑自己填（不让我代写）。
- 连续两天累，聚焦「mini-GPT 落地」这一件主事，别又把语言 / DL / 元认知同时铺开。
- （可选）把「人 ≈ RLHF 模型」类比展开成一篇短文，沉淀进笔记。
